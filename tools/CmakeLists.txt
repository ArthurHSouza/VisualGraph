set(LIBRARY_SOURCES "AssetManager.cpp")
set(LIBRARY_HEADERS "AssetManager.hpp")

add_library(
	${LIBRARY_TOOLS} STATIC
	${LIBRARY_SOURCES}
	${LIBRARY_HEADERS}
)

target_include_directories(
	${LIBRARY_TOOLS} PUBLIC
	"./"
)

target_link_libraries(
	${LIBRARY_TOOLS} PUBLIC
	sfml-graphics
)


